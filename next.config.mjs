/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   // Required for Next.js 13.4.x - can be removed when upgrading to 13.5+
  //   appDir: true,
  // },
}

export default nextConfig
